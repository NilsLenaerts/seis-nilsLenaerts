#pragma once

#include <vector>
#include <string>

namespace constants{
    // wat identifiers
    std::vector<char> WAT_IDENTIFIERS = std::vector<char>{'!', '#', '$', '%', '&', '\'', '*', '+', '-', '.', '/', ':', '<', '=', '>', '?', '@', '\\', '^', '_', '`', '|', };
}